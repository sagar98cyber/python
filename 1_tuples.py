#Declaration of TUPLES
numbers = (1,2,3)

#TUPLES are immutable so you cannot change the value of the tuple
#numbers[0] = 10

print(numbers[0])
#you can only read and access the items or values of the tuples
